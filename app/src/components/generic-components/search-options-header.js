export default function SearchOptionsHeader({title}){

    return(
            <div className="row">
                <div className="col-sm">
                    <h1>{title}</h1>
                </div>
            </div>
    );
}